[
  {
    "id": "pk-map-refresh",
    "schedule": "30 8 * * 1",
    "prompt": "Refresh the Rivalia PK map: rescrape Aeternum player profiles (recursive crawl to closure), rebuild the who-kills-who graph + danger scores + relationship graph, regenerate the reverse map and publish it to GitHub Pages (commit map/index.html and push).",
    "type": "script",
    "scriptFile": "personal/rivalia/cron/pk-map-refresh.sh",
    "publish": "github-pages",
    "automation": "github-actions",
    "workflow": ".github/workflows/refresh-map.yml",
    "note": "Mon 08:30 UTC (~10:30 CEST). Personal hobby (Rivalia). Pipeline: build_seed.py -> crawl_pk.py -> build_pkdata.py -> build_relationships.py -> build_insights.py -> INLINE=1 build_html.py -> commit map/index.html to main. Scraping rivaliaonline.com is public (no auth). Publishing is GitHub Pages from main (was OneDrive/Midway on the old work-PC setup). The weekly automation is the GitHub Action .github/workflows/refresh-map.yml (NOT a Claude Routine: routine sessions can't push to main or merge PRs). cron/pk-map-refresh.sh is kept for manual/local runs. This JSON is just a record of the schedule + pipeline."
  }
]
